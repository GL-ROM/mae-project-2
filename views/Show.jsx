const React = require('react');

class Show extends React.Component {
    render(){
        const {} = this.props;

        return (
            <Default>
                <h1>Show page</h1>
            </Default>
        )
    }
};

module.exports = Show;